# Prisma & Kysely

Personal learnings from working with Prisma and Kysely together — when to reach for each, the gotchas, and fixes for issues hit along the way.

## In This Section

- **Shadow DB FK Dependency Error** — resolving foreign-key dependency failures in Prisma's shadow database.
- **Prisma & Kysely Integration Issue** — making the two libraries coexist cleanly.
- **Persist Hydration SSR Error** — debugging a hydration mismatch in server-side rendering.
- **Prisma BigSerial Alter Column** — altering a column to `bigserial` with Prisma migrations.
